runtime: python27
api_version: 1
threadsafe: true
instance_class: B1
manual_scaling:
  instances: 1

handlers:
- url: /static
  static_dir: static
  secure: always
- url: /.*
  script: main.app
  secure: always

libraries:
- name: ssl
  version: latest
